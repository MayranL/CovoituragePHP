{# templates/utilisateur/inscription.html.twig #}

{% include 'base.html.twig' %}

{% block content %}
    {% if error is defined and error %}
        <div class="alert alert-danger">
            {{ error.messageKey|trans(error.messageData, 'security') }}
        </div>
    {% endif %}
    <nav style="top: 5rem;" class="absolute left-5 flex dark:text-white dark:bg-gray-900 " aria-label="Breadcrumb">
        <ol class="inline-flex items-center space-x-1 md:space-x-3">
            <li class="inline-flex items-center">
                <a href="{{ path('dashboard') }}" class="inline-flex items-center text-sm font-medium hover:text-gray-70">
                    <svg aria-hidden="true" class="w-4 h-4 mr-2" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M10.707 2.293a1 1 0 00-1.414 0l-7 7a1 1 0 001.414 1.414L4 10.414V17a1 1 0 001 1h2a1 1 0 001-1v-2a1 1 0 011-1h2a1 1 0 011 1v2a1 1 0 001 1h2a1 1 0 001-1v-6.586l.293.293a1 1 0 001.414-1.414l-7-7z"></path></svg>
                    Accueil
                </a>
            </li>
            <li>
                <div class="flex items-center">
                    <svg aria-hidden="true" class="w-6 h-6 text-gray-400" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd"></path></svg>
                    <a href="{{ path('modifier_annonce', {'id' : id}) }}" class="ml-1 text-sm font-medium hover:text-gray-700  md:ml-2 ">Modifier un trajet</a>
                </div>
            </li>
        </ol>
    </nav>
    <div style="margin-top: 5vw">
    <section class="">
        <div class="flex flex-col items-center justify-center px-6 py-8 mx-auto md:h-screen lg:py-0">
            <div class="w-1/2 bg-white rounded-lg shadow dark:border md:mt-0 sm:max-w-md xl:p-0 dark:bg-gray-800 dark:border-gray-700">
                <div class="p-6 space-y-4 md:space-y-6 sm:p-8">
                    <h1 class="text-xl font-bold leading-tight tracking-tight text-gray-900 md:text-2xl dark:text-white">
                        Mise à jour du trajet
                    </h1>
                    <form class="space-y-4 md:space-y-6" method="POST" action="{{ path('modifier_annonce', {'id': id}) }}">
                        {{ form_start(form) }}
                        {{ form_row(form.villeDepart) }}
                        {{ form_row(form.villeArrive) }}
                        {{ form_row(form.date) }}
                        {{ form_row(form.prix) }}
                        {{ form_row(form.modeleV) }}
                        {{ form_row(form.nbplace) }}
                        <div class="flex justify-center items-center">
                        {{ form_end(form) }}
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </section>
    </div>

{% endblock %}
